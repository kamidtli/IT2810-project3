import Card from '../../../../src/components/Card/Card'

describe('Test for card component', () => {
      it('Checks the card component', () => {
        const result = MediaCard()
        expect(result).to.deep.equal({
            
        })
      })
  })
